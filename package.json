{
  "name": "booksetu",
  "version": "1.0.0",
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install && node node_modules/vite/bin/vite.js build",
    "start": "cd server && node src/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
